server {
    listen 80;
    server_name localhost; # Adjust if needed for your domain

    # Root directory for static assets
    root /usr/share/nginx/html;
    index index.html index.htm;

    # Standard SPA Nginx configuration:
    # Try to serve the requested file directly,
    # if not found, serve index.html (which will handle client-side routing).
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Add headers for caching, security, etc.
    # location ~* \.(?:ico|css|js|gif|jpe?g|png)$ {
    //     expires 1y;
    //     add_header Cache-Control "public";
    // }

    # Optional: Gzip compression
    # gzip on;
    # gzip_vary on;
    # gzip_proxied any;
    # gzip_comp_level 6;
    # gzip_buffers 16 8k;
    # gzip_http_version 1.1;
    # gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # Optional: Proxy API requests if Nginx is also acting as a reverse proxy
    # This is an alternative to the Vite dev server proxy, for production.
    # location /api/ {
    //     proxy_pass http://your_backend_service_url; # e.g., http://backend:3001
    //     proxy_set_header Host $host;
    //     proxy_set_header X-Real-IP $remote_addr;
    //     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    //     proxy_set_header X-Forwarded-Proto $scheme;
    // }
}
